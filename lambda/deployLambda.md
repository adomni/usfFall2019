
1. **pip install virtualenv**
2. **cd src**
3. **virtualenv ./venv**
4. Switch to your virtual env: **source venv/bin/activate**
6. **pip install -r requirements.txt**
7. **./deploy.sh**
8. Upload the zip file to lambda aws console
